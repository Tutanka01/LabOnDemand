# Test dependencies for LabOnDemand backend
# Install with: pip install -r requirements.txt -r requirements-test.txt
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
fakeredis[lua]==2.38.0     # full in-memory Redis (Lua scripts, NX/PX locks, counters) for tests
email-validator>=2.0       # required by pydantic[email] (not included in requirements.txt)
# bcrypt must be pinned to match passlib compatibility (already in requirements.txt):
# bcrypt>=4.0.1,<4.1
